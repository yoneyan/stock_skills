[project]
name = "stock-skills"
version = "0.1.0"
description = "割安株スクリーニングシステム"
requires-python = ">=3.10"
dependencies = [
    "yfinance>=1.1.0",
    "pyyaml>=6.0",
    "numpy>=1.24.0",
    "python-dotenv>=1.0.0",
    "pytest>=7.0",
    "networkx>=3.0",
]

[tool.uv]
package = false
